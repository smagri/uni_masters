//random numbers random library in c++

// put all in one file, just for learning.


vector<

#include <chrono>

  addValues(vector<double> &numVec){
  seed = chrono::system_clock::now().time_since_epoch().count;
  ...


    std::uniform_int_distributn<unsigned int> num_elements_distri..(0,10)
      std::normal_distri...(8.0, 4.0);

    uint num_elements_to_add == num_elements_dist..n(generator);
    for (i=0; i < num_elements_to_add; i++){
      numVec.push_back(value_distribution(generator));
    }
}


  double fn1(real or doubles numbers pass by reference vector){

  // populate random numbers of elements of vector, select numbers
  // from with a uniform distribution (0-10)
  //
  // obj1 -use c++11 in CMakeList.txt
  //
  //how many elements to add

  // obj2 - select numbers from a use a gaussian, aka normal distribution.
  // mena=8; std dev 4.
  //
  //reference-> other-><random>->gausiann
  //
  // random number c++ gaussian into google
  //
  // values
};


double fn2(vector){

// re-arrange vector accoding to bubble_sort

}


printValues(
	    for vector<double>:: iterator it = numVec.begin():: it < numVec.end(); it++){
  cout<<...
}

bubbleSprt(vector<double> &numVec){
 // cut and paste
  int n = numVec.size();
    // swap values using temporary vairiable

  ...if (numVec.at(i-1) > numVec.at(i){


      ...
      temp = numVec.at(i-1);
      numVec.at(i) = temp;
      }


int main(){

  fn1();
  // print the vector
  
  fn2();
  // print the vector
  t(numVec
  // modify code to use a vector of integers instead
  //
  // Templates???

  vector<double> numVec;
  addValues(numVec)
    printVals
    bubbleSort(&numVec);
    //swap fn in c++ swap values in two vectors
    // swap fn in c++ does NOT swap values in the same vector
  // make a vector
// push_back to put values  in the vector
  // loop number of times, ie int times
  
}


