//
// a3_help::RequestGoal Class Implementation/definition.
//
//
// Class fn's or methods(aka member fn's) are defined here.

#include "a3_help/RequestGoal.h"


// a3_help::RequestGoal::RequestGoal(){
// };

// a3_help::RequestGoal::~RequestGoal(){
// };

// LATER: using namespace "a3_help/RequestGoal"???;

// Default constructor does  not exist here, as that  would require no
// input or output paramters???
// RequestGoal::RequestGoal(Request &req, Response &res){

//   // The robot  is initialised  at the  centre of  the ogMap.   And is
//   // moved via  the position  and orientation/  pose specified  in the
//   // Request struct.
//   //
//   double req_.x = 0.0;
//   double req_.y = 0.0;
//   double req_.angle = 0.0;
//   double req_.scanTime = 10;
  
//   bool res_.ack = false; // if service request succeeds return true

// }


// Constructor for a3_help::RequestGoal object.
//
// But perhaps  this is not  required as no  member fn's exist  to use
// them.   However,  maybe  do  this for  safety  down  the  trak(like
// default: option in case statement)???

// RequestGoal::RequestGoal(Request &req, Response &res){

//   // This  constuctor, or  method/member fn  is required  so code  not
//   // within  this class  can access  member variables  safely, without
//   // having to understand all our code.
//   //
  
//   double req_.x = req.x;
//   double req_.y = req.y;
//   double req_.angle = req.angle;
//   double req_.scanTime = req.scanTime;
  
//   bool res_.ack = false; // if service request succeeds return true
  
//   return res_.ack;
  
// }

// a3_help::RequestGoal::RequestGoal(){
//   // Default constructor ie default => no return type or parameters.

// }

// a3_help::RequestGoal::~RequestGoal(){
//   // Default destructor ie default => no return type or parameters.
  
// }

// namespace a3_help
  
